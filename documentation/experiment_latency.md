# Experiment "Vllm latency"

Hypothesis: Using Vllm instead of Ollama would improve latency.

- Strategy: vllm (the system prompt and user prompt is combined to one prompt)
- Strategy: vllm-slim (the system prompt and user prompt is combined to one prompt)